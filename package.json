{
  "name": "cat-cafe-project",
  "version": "1.0.0",
  "description": "Este es un proyecto **API NestJS** con **Prisma + MySQL** y **frontend en Angular**.  \r Permite al usuario crear una cuenta, loguearse y hacer reservas en el cat café.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "frontend:build": "npm run build -w frontend",
    "frontend:deps": "npm install -w frontend",
    "backend:deps": "npm install -w backend",
    "backend:start": "npm run start -w backend"
  },
  "author": "",
  "license": "ISC",
  "workspaces": [
    "backend",
    "frontend"
  ]
}
